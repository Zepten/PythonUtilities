import argparse

# Argument parser
parser = argparse.ArgumentParser(description='IP-calculator')

# Positional arguments
parser.add_argument('')

# Optional arguments
parser.add_argument('')

# Argument parsing
args = parser.parse_args()